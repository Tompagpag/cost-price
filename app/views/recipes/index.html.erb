<div class="container p-5">
  <h1 class="pb-3">Mes recettes</h1>
  <% @recipes.each do |recipe| %>
    <div>
      <h3><%= link_to recipe.name, recipe_path(recipe) %> - Coût : <%= recipe.total_cost.truncate(2) %></h3>
    </div>

  <% end %>
</div>
