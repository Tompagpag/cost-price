<div class="container p-5">
  <h1>Mes recettes</h1>
  <% @recipes.each do |recipe| %>
    <h3><%= link_to recipe.name, recipe_path(recipe) %></h3>
    <h3>Coût : <%= recipe.total_cost %></h3>
  <% end %>
</div>
